export class OrderSubsystem {
    createOrder(productId: string, quantity: number): string {
        // Crear un nuevo pedido con el producto y la cantidad especificados
        console.log('Orden creada: 001');
        return '001';
    }
}